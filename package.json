{
  "name": "tmsf-service-seed",
  "version": "0.0.8",
  "description": "a microservice seed project built upon tmsf",
  "main": "index.js",
  "scripts": {
    "test": "echo 'no tests at the moment'",
    "start": "NODE_ENV=development node ./",
    "clean": "rm -rf node_modules/",
    "start:mongo": "docker-compose -f docker-compose-mongo.yml up -d",
    "stop:mongo": "docker-compose -f docker-compose-mongo.yml down",
    "start:dev": "NODE_ENV=development node ./",
    "start:prod": "NODE_ENV=production node ./",
    "start:docker": "docker-compose up -d",
    "stop:docker": "docker-compose down"
  },
  "author": "Wolfgang Beer <wolfgang.appdev@gmail.com>",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/wolfgang-appdev/tmsf-service-seed.git"
  },
  "license": "MIT",
  "dependencies": {
    "tmsf": "0.0.8"
  }
}
